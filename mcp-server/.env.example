# Database Configuration
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Optional: Specific user ID to filter tasks/sessions (leave empty for multi-user support)
DEFAULT_USER_ID=
