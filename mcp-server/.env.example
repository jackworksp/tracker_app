# Database Configuration
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# MCP API Key - Generate this in the Vela app: Profile → MCP Integration → Generate Key
API_KEY=your_mcp_api_key_here
